{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"ignoreCommand": "sh -c 'echo \"$VERCEL_GIT_COMMIT_MESSAGE\" | grep -q \"\\[deploy\\]\" && exit 1 || exit 0'",
	"buildCommand": "npm run build",
	"outputDirectory": "dist"
}
